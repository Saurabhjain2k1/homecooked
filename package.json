{
  "name": "homecooked-server",
  "version": "1.0.0",
  "description": "1) Clone this repo and `cd` into the project directory. 2) Run `npm install` to install all the dependencies.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
